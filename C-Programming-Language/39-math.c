#include <stdio.h>
#include <math.h> // Include the math library

int main() {
    int a = 144;
    int b = 12.5;
    printf("%f", sqrt(a));
    printf("\n%f", ceil(1.4));
    printf("\n%f", floor(1.4));
    return 0;
}

